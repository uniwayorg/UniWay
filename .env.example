# Database — Neon (Postgres + PostGIS)
DATABASE_URL="postgres://user:password@ep-example-123456.us-east-2.aws.neon.tech/uniway?sslmode=require"
# PGURL is required by 'ley' to run migrations
PGURL="postgres://user:password@ep-example-123456.us-east-2.aws.neon.tech/uniway?sslmode=require"

# Cloudflare R2 — PMTiles tile hosting
# R2_ACCOUNT_ID=""
# R2_ACCESS_KEY_ID=""
# R2_SECRET_ACCESS_KEY=""
# NEXT_PUBLIC_R2_PUBLIC_URL="https://pub-xxxxx.r2.dev"

# Sentry (add before production deploy)
# NEXT_PUBLIC_SENTRY_DSN=""

# No auth required in V1 — API routes are unauthenticated
# Rate limiting is the only access control
